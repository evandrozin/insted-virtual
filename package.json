{
  "name": "insted-virtual-campus",
  "private": true,
  "version": "1.0.0",
  "description": "Maquete virtual 3D e controle de presenca em tempo real do Insted",
  "scripts": {
    "build": "npm --prefix apps/web-3d-frontend install && npm --prefix apps/web-3d-frontend run build",
    "dev": "npm --prefix apps/web-3d-frontend run dev"
  }
}
