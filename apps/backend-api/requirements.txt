# Python 3.11+ (validado em 3.14). pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.49
pydantic>=2.10
httpx>=0.28
websockets>=14.0

# Estado compartilhado entre instancias (obrigatorio quando REDIS_URL existe;
# sem ele o sistema roda em memoria e este pacote fica ocioso).
redis>=5.0

# Cadastro de salas em Postgres (obrigatorio quando DATABASE_URL existe;
# sem ele a topologia vem do seed extraido das plantas).
asyncpg>=0.30

# Tokens de sessao para a edicao do cadastro (obrigatorio com JWT_SECRET).
PyJWT>=2.8

# Base de fusos horarios. Nao e opcional: a grade e hora de parede local e o
# zoneinfo da biblioteca padrao nao carrega base nenhuma sozinho. Windows nunca
# tem /usr/share/zoneinfo, e as imagens enxutas de serverless tambem nao - sem
# este pacote o app nao importa.
tzdata>=2025.1

# Somente para os testes: emula um servidor Redis em processo.
# fakeredis>=2.30
